Function bloquearAcesso()

    Dim nomearq, caminho, cabeca, resultado, mat As String
    Dim a As Integer
    
    nomearq = "bloquearAcesso.txt"
    caminho = "P:\ACESSO BLOQUEADO\"
    a = FreeFile
    
    'cabeçalho das instruções
    cabeca = "UPDATE dbo.PESSOA SET CD_SITUACAO_PESSOA = 12 WHERE NU_MATRICULA = "
    
    'inicialização das variáveis
    resultado = ""
    
    For rep = 1 To 445
        
        mat = Range("A" & rep).Value
        
        If (mat <> "") Then 'se a célula não está vazia
        
            'se a variável 'resultado' está vazia, apenas inserimos a instrução, caso já tenha sido preenchida, adicionamos um ponto e vírgula e pulamos a linha
            If (resultado <> "") Then
                resultado = resultado & ";" & vbCr & cabeca & mat
            Else
                resultado = cabeca & mat
            End If
          
        End If
        
    Next rep
    
    'adicionamos ponto e vírgula ao final da última instrução
    resultado1 = resultado & ";"
    
    'escrevemos o resultado no arquivo de texto
    Open caminho & nomearq For Output As #a
        Print #a, resultado
    Close #a
        
End Function


